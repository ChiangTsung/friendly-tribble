# 2026-03-11 - Daily Memory

## 🌤️ 上午整理（12:00）

### 完成的任务
- 触发并执行「Daily Memory 整理 - 中午」定时任务。
- 检查当日 memory 文件状态：`memory/2026-03-11.md` 原本不存在，已创建。
- 完成上午阶段的首轮工作记录归档。

### 技术决策
- 在记忆检索工具不可用时（`memory_search` 触发配额限制），先落盘“已确认事实 + 当前可见上下文”，避免中午整理任务阻塞。
- 继续采用“中午先记事实，晚间再补全细节”的分段整理策略。

### 用户反馈
- 上午无新的直接用户反馈记录（当前可见上下文中）。

### 犯的错误 / 风险
- `memory_search` 在本次执行中返回 429（embedding 配额耗尽），导致无法通过语义检索补充上午跨会话细节。
- 当前记录基于本轮任务可见信息，可能遗漏其他会话中的上午事件，待晚间整理时补充。

### 上午简要总结
上午以例行记忆维护为主：完成了中午定时整理触发、当日 daily memory 文件创建与首版记录。系统层面出现一次检索配额限制，但任务未中断，已采用保守记录策略保证连续性。

---

## 🧭 用户工作流指令（晚间）
- 用户消息要**秒回**；任何 >5s 的操作走后台，前台只做快速指令（必要时用 message 先回执）。
- 用**第一性原理**思考：从原始需求/问题本质出发，不假设用户清楚路径。
- 真干活开 **tmux** 交给 **Codex**：任务小而明确，附验收标准，让它自改自跑测试。
- 每次 **heartbeat** 必须主动检查 Codex 进度；数据连续不变=异常信号 → kill 卡住进程并重发任务，不能只报数字。
- heartbeat 用**便宜模型**（如 step-3.5-flash）以控成本。
- 上下文尽量控制在 **100k** 内；主动做 **compaction**。
- **commit early, commit often**：改了就 commit + push。
- Codex 可能缺环境变量（API key/proxy）：启动前确认；任务里要求 git commit。
- 工作流代码要干净：有用工具维护，临时脚本用完删掉或 ignore。
- 我是在**值班巡逻**：没人叫也要查 Codex/进度/异常/卡住，主动发现问题更重要。

---

## 🌙 晚间补全与午夜归档（00:00）

### 下午/晚间新增要点（已补录）
- 将全天主线明确为「值班巡逻工作流」：目标从被动响应升级为主动发现问题。
- 形成一套可执行的稳定规则：
  - >5s 操作后台化（秒回前台回执）；
  - heartbeat 不只报数字，必须做“是否卡住”的状态判断；
  - 卡住即 kill + 重发（附清晰验收标准）；
  - 控成本（便宜模型）+ 控上下文（<100k，必要时 compaction）；
  - commit/push 走高频小步提交。
- 将工程前置检查纳入固定动作：开工先看环境变量（API key / proxy），避免 Codex 空跑。

### 当日状态判定
- 类型：流程治理日（执行规范定型），非功能开发日。
- 产出：新增并固化一套值班与执行纪律，可直接复用到后续 heartbeat / cron / 并发任务处理中。

### 清理与完成标记
- [x] 当天 daily memory 已补全（含晚间新增）
- [x] 关键长期记忆已提炼并同步到 `MEMORY.md`
- [x] 本轮午夜整理完成

### 待跟进（次日）
- [ ] 在实际 heartbeat 中持续验证“卡住检测→kill→重发”闭环执行效果。
- [ ] 观察“秒回+后台执行”是否降低漏消息体感与响应焦虑。
